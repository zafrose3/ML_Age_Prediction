apiVersion: v1
kind: Service

metadata:
  name: age-prediction-service

spec:
  type: NodePort

  selector:
    app: age-prediction

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30001